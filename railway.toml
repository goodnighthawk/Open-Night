[build]
builder = "RAILPACK"

[deploy]
startCommand = "PYMMO_SHARED_DATA=/tmp/open-night-shared python -u server.py --host 0.0.0.0 --port $PORT --memory-db --no-discovery --name \"Open Night Internet Server\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
