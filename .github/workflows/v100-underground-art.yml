name: Hold v1.0 Underground art

on:
  workflow_dispatch:

jobs:
  held:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Underground is intentionally blank/held. Ground manholes remain future crouch-to-Underground transition markers.'
